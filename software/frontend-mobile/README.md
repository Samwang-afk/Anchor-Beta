# anchor-beta Mobile Frontend

移动端优先的主演示界面，负责思绪整理、任务启动、硬件联动、全屏专注倒计时、完成鼓励和成长收藏。

```bash
npm install
npm run dev
```

默认开发端口为 `5173`，`/api` 和 `/ws` 会代理到 `localhost:3001`。

更多说明见 `../../README.md` 和 `../../docs/software/frontend.md`。
