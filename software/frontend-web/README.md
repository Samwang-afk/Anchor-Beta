# FocusDock Web Frontend

横屏/桌面演示界面，适合在电脑浏览器上展示 FocusDock 的任务、专注和统计信息。

```bash
npm install
npm run dev
```

`/api` 和 `/ws` 会代理到 `localhost:3001`。

更多说明见 `../../README.md` 和 `../../docs/software/frontend.md`。
